import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.cubanitos.patagonia",
  appName: "Cubanitos Patagonia",
  webDir: "APP_CUBANITOS_PATAGONIA",
  server: {
    androidScheme: "https"
  }
};

export default config;
