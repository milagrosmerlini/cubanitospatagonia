{
  "name": "cubanitos-patagonia-native",
  "private": true,
  "version": "1.0.0",
  "description": "Contenedor nativo offline-first de Cubanitos Patagonia.",
  "scripts": {
    "native:build": "esbuild native-storage.ts --bundle --format=iife --target=es2020 --outfile=APP_CUBANITOS_PATAGONIA/native-storage.js",
    "cap:sync": "cap sync",
    "android:add": "cap add android",
    "android:open": "cap open android",
    "android:build": "cap build android"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^8.1.1",
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/network": "^8.0.1",
    "@capacitor/share": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  }
}
